# Optional local-machine overrides for scripts/run_pipeline.sh.
# Runtime behavior belongs in tello_ws/src/tello_defect_pipeline/config/pipeline.yaml.
# Relative CONFIG_FILE and VENV_DIR values are resolved from the repo root.
CONFIG_FILE=tello_ws/src/tello_defect_pipeline/config/pipeline.yaml
# ROS_SETUP=/opt/ros/jazzy/setup.bash
# VENV_DIR=tello_ws/venv
# TERMINAL_CMD=gnome-terminal
