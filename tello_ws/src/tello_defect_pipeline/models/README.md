# Model Weights

Place the trained PyTorch weights at `model.pth` in this directory, or pass a different path to the detector node with the `model_path` ROS parameter.

The actual `.pth` file is intentionally not included in source control.
